{
  "name": "benchmark",
  "private": true,
  "version": "0.1.0",
  "type": "module",
  "scripts": {
    "bench": "node index.js",
    "bench:install": "npm install; npm install .. --install-links"
  },
  "dependencies": {
    "@noble/curves": "file:.."
  }
}
